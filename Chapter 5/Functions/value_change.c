#include<stdio.h>
int main(){
    // we can change the value of variable in c but we can't change the function value..
    int a = 10;

    a = 5;
    printf("The value of a is %d", a);
    return 0;
}