#include<stdio.h>   //Standard input output
#include<math.h> 


int main(){
    
    int a = 10, b = 25, c = 15;

    printf("adding the value of a and b %d\n", a+b);
    printf("adding the value of a and b %d\n", a-b);
    printf("adding the value of a and b %d\n", a*b);
    printf("adding the value of a and b %d\n", a/b);
    printf("modulas the value  %d\n", 10%3);
    

    // What is mean by expression?
    // Expression is the combination of operand and operator.

    // Ex --> 1 - 5
    // Assignment opertor = 

    // int z = b*a;
    // printf("%d\n", z);

    // printf("%d", 5 % -2);

    // float c=2, d=1;
    // int z = 0.4;

    // float k = 3.0/9;

    // printf("%f", k);

    // printf("%d", 2*2*2);
    
    // printf("%f", pow(2, 3));

    // int x=2, y=3;

    // printf("%d", 8*3 / 3*2);
    // 24/3*2
    // 8*2 = 16

    // printf("%d", (8*3) / (3*2));
    // 24/6 = 4

    // printf("%d", 2*(3-1));

    return 0;
}
