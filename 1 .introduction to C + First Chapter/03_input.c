#include <stdio.h>

int main()
{

    int i, a;
    printf("Enter a number: ");
    scanf("%d", &i);
    printf("Enter a 2nd number: ");

    scanf("%d", &a);

    printf("The sum of two number is: %d", i + a);

    return 0;
}