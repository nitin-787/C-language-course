#include<stdio.h>

int main(){
    
    // Ques no.1
    // int l, b;

    // printf("Enter length: ");
    // scanf("%d", &l);
    // printf("Enter breadth: ");
    // scanf("%d", &b);

    // printf("The are of rectangle is: %d", l*b);

    // Ques no.2
    // formula: circle- 3.14*r*R | Volume: 3.14*r*R*h

    // float radius=10, p=3.14, h=10;
    // printf("Area of circle: %f\n", p*radius*radius);
    // printf("Volume of cylinder: %f", p*radius*radius*h);

    // Ques no.3 -
    // formula: p*t*r/100

    // int p=100, t=1, r=15, SI;
    // SI = p*t*r/100;

    // printf("Interest Ammount: %d\n", SI);
    // printf("Total ammount to be paid: %d", SI+p);

    // Ques no.4 
    // Formula: (C*9/5) + 32

    // float C=37;
    // printf("Celsius to Fahrenheit: %f", (C*9/5) + 32);

    return 0;
}